

<nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <a class="navbar-brand" href="/">
        <%= fa_icon "medkit", text: "medtracker" %>
      </a>  

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li class="active">
          <%= link_to 'Doctors', doctors_path %>
        </li>
        <li class="active">
          <%= link_to 'Patients', patients_path %>
        </li>

      </ul>

      <ul class="nav navbar-nav navbar-right">
<!--         <form class="navbar-form navbar-left" role="search">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="type in a category">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form> -->

        <% if current_user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user fa-fw"></i> 
              <% if current_user.type == "Patient" %>
                Welcome <%= current_user.name %>
              <% else %>
                Welcome Dr. <%= current_user.last_name %>
              <% end %>
            <span class="caret"></span>
          </a>

          <ul class="dropdown-menu" role="menu">
            <% if current_user.type == "Patient" %>
              <li>
                <%= link_to '  update my profile', edit_patient_path(current_user), class: "fa fa-cogs" %>
              </li>
              <% else %>
              <li>
                <%= link_to '  update my profile', edit_doctor_path(current_user), class: "fa fa-cogs" %>
              </li>
            <% end %>

            <li>
             <%= link_to '  log me out', session_path, method: :delete, class: "fa fa-sign-out" %>
            </li>
          </ul>
          
        </li>

        <% else %>
        <li class="dropdown">

          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-user fa-fw"></i> Please log in
            <span class="caret"></span>
          </a>

          <ul class="dropdown-menu" role="menu">
            <li>
              <%= link_to 'Log in', new_session_path  %>   
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to "  Sign up", new_user_path, class: "fa fa-sign-in" %>
            </li>

          </ul>

        </li>      
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
